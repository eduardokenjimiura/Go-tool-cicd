version: 0.2

#env:
  #variables:
     # key: "value"
     # key: "value"
  #parameter-store:
     # key: "value"
     # key: "value"

phases:
  install:
    commands:
     - export  GO111MODULE=off
      # - command
      # - command
  #pre_build:
    #commands:
      # - command
      # - command
  build:
    commands:
        - ls
        - go build -o  main   
        
        - ./main input.json taskdefinition.json arn:aws:iam::036315699000:role/ecsTaskExecutionRole
        - cd $CODEBUILD_SRC_DIR_SourceArti
        - ls
        - cd ..
        - cd $CODEBUILD_SRC_DIR_local
        - ls
        
        - echo local
       
      # - command
      # - command
  #post_build:
    #commands:
      # - command
      # - command

#artifacts:
  #files:
    # - location
    # - location
  #name: $(date +%Y-%m-%d)
  #discard-paths: yes
  #base-directory: location
#cache:
  #paths:
    # - paths